# trading-SySTEM — TEST environment overrides
# Test: 僅起 Streamlit UI(8601)；KGI/sim backend 沿用 prod（port 5100/5101）。

GS_ENV=test
TRADING_ENV=test
KGI_PORT=5100
SIM_PORT=5101
# Test Streamlit 連接 prod backend（模擬盤測試）。
# 若需完全隔離，須先啟動獨立的 kgi_sim_server：
#   cd trading-SySTEM && venv\Scripts\python.exe kgi_sim_server.py
