# trading-SySTEM — PROD environment overrides
# Prod: run.ps1 一鍵起 KGI(5100) + sim(5101) + Streamlit(8501)。

GS_ENV=prod
TRADING_ENV=prod
KGI_PORT=5100
SIM_PORT=5101
# KGI_ACCOUNT=   # 券商帳號（機密）
# KGI_PASSWORD=  # 券商密碼（機密）
