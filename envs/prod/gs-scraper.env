# gs-scraper — PROD environment overrides
# ⚠️ Test 環境不支援：ui/search/app.py L262 hardcodes port 5050，無 env override。
# Prod 只：port 5050 hardcoded，此檔僅存業務相關 env vars。

GS_ENV=prod
# QD_DB_PATH=  # 若需覆蓋資料庫路徑
